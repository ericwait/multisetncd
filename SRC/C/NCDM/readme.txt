% readme.txt

see ../../../License.txt for details

this program is an MPI implementation of the normalized compression distance multisets (NCDM). 

it requires MPIch, freely available from Argonne national labs.

it also requires bzip2, compiled as a static library (included in ../bz2static)

NCDM.c is the main entry point
